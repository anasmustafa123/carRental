import React from "react";
import { createContext } from "react";
const dashContext = createContext("user");

constDashProvider = ({ children }) => {

    



  return <UserProvider.Provider></UserProvider.Provider>;
};

export default function website_context() {
  return <div>website_context</div>;
}
